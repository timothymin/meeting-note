cask "meeting-note" do
  version "VERSION"
  sha256 "SHA256"

  url "https://github.com/OWNER/REPOSITORY/releases/download/v#{version}/MeetingNote-macOS-arm64.zip"
  name "Meeting Note"
  desc "Private on-device MLX meeting transcription"
  homepage "https://github.com/OWNER/REPOSITORY"

  depends_on arch: :arm64
  depends_on macos: ">= :sonoma"

  app "MeetingNote.app"

  zap trash: [
    "~/Library/Preferences/dev.meetingnote.app.plist"
  ]
end
